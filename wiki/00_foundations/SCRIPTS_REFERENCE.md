---

**File:** `scripts/analyze_constellation_shapes.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Not yet audited.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_constellation_shapes()` - *Functional interface.*










**File:** `scripts/analyze_constellation_shapes.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_constellation_shapes()` - *Functional interface.*




**File:** `scripts/analyze_eclipse_stones.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `datetime.timedelta`
* `datetime.timezone`
* `math`
* `openastro2.openastro2.openAstro`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `to_zodiacal_string()` - *Functional interface.*
**Exposes:** `get_stone_idx()` - *Functional interface.*
**Exposes:** `analyze()` - *Functional interface.*
**Exposes:** `get_deg()` - *Functional interface.*




**File:** `scripts/analyze_matrix_properties.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.timedelta`
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_matrix()` - *Functional interface.*




**File:** `scripts/analyze_pattern_stats.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_stats()` - *Functional interface.*




**File:** `scripts/analyze_seed_hotspots.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_hotspots()` - *Functional interface.*




**File:** `scripts/analyze_seeds.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_seeds()` - *Functional interface.*




**File:** `scripts/analyze_spiral_harmonics.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `math`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `gcd_list()` - *Functional interface.*
**Exposes:** `analyze_harmonics()` - *Functional interface.*




**File:** `scripts/analyze_ternary_patterns.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections.Counter`
* `collections.defaultdict`
* `datetime.timedelta`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_patterns()` - *Functional interface.*




**File:** `scripts/analyze_trigram_tensor.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.timedelta`
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `trigram_to_charge()` - *Functional interface.*
**Exposes:** `trigram_to_id()` - *Functional interface.*
**Exposes:** `analyze_tensor()` - *Functional interface.*




**File:** `scripts/analyze_void_distribution.py`

**Role:** `[Scout]`

**Purpose:** Analyze the actual Void Star distribution from the planetary lattices.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections.defaultdict`
* `json`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `calculate_seeds_for_planet()` - *Calculate seed coordinates for a planetary lattice.*




**File:** `scripts/analyze_wall_patterns.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.adyton.models.kamea_cell.KameaCell`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_wall_index()` - *Functional interface.*
**Exposes:** `analyze_air_tablet()` - *Functional interface.*




---

**File:** `scripts/apply_asterism_style.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `text_to_grid()` - *Functional interface.*
**Exposes:** `render_asterism()` - *Functional interface.*
**Exposes:** `process_match()` - *Functional interface.*
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_constellation_shapes.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_constellation_shapes()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_eclipse_stones.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `datetime.timedelta`
* `datetime.timezone`
* `math`
* `openastro2.openastro2.openAstro`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `to_zodiacal_string()` - *Functional interface.*
**Exposes:** `get_stone_idx()` - *Functional interface.*
**Exposes:** `analyze()` - *Functional interface.*
**Exposes:** `get_deg()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_matrix_properties.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.timedelta`
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_matrix()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_pattern_stats.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_stats()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_seed_hotspots.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_hotspots()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_seeds.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_seeds()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_spiral_harmonics.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `math`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `gcd_list()` - *Functional interface.*
**Exposes:** `analyze_harmonics()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_ternary_patterns.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections.Counter`
* `collections.defaultdict`
* `datetime.timedelta`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_patterns()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_trigram_tensor.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.timedelta`
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `trigram_to_charge()` - *Functional interface.*
**Exposes:** `trigram_to_id()` - *Functional interface.*
**Exposes:** `analyze_tensor()` - *Functional interface.*


---

**File:** `scripts/attic/analyze_void_distribution.py`

**Role:** `[Scout]`

**Purpose:** Analyze the actual Void Star distribution from the planetary lattices.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections.defaultdict`
* `json`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `calculate_seeds_for_planet()` - *Calculate seed coordinates for a planetary lattice.*


---

**File:** `scripts/attic/analyze_wall_patterns.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.adyton.models.kamea_cell.KameaCell`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_wall_index()` - *Functional interface.*
**Exposes:** `analyze_air_tablet()` - *Functional interface.*


---

**File:** `scripts/attic/debug_doc_collections.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.document_manager.services.document_service.document_service_context`
* `sys`
* `traceback`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/attic/debug_doc_tags.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.document_manager.services.document_service.document_service_context`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/attic/debug_natal_julian_day.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `os`
* `pillars.astrology.models.AstrologyEvent`
* `pillars.astrology.models.ChartRequest`
* `pillars.astrology.models.GeoLocation`
* `pillars.astrology.services.OpenAstroService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `debug_julian_day()` - *Functional interface.*


---

**File:** `scripts/attic/generate_adyton_constellations.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `random`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_constellations()` - *Functional interface.*


---

**File:** `scripts/attic/generate_annealed_constellations.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `random`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_kmeans_constellations()` - *Functional interface.*
**Exposes:** `is_connected()` - *Functional interface.*
**Exposes:** `is_valid_shape()` - *Functional interface.*
**Exposes:** `calculate_compactness()` - *Functional interface.*


---

**File:** `scripts/attic/generate_archimedean_data.py`

**Role:** `[Scout]`

**Purpose:** Fetch canonical Archimedean solid data from dmccooey.com and emit Python module.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `__future__.annotations`
* `ast`
* `math`
* `operator`
* `pathlib`
* `requests`
* `typing.Dict`
* `typing.List`
* `typing.Tuple`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `parse_solid()` - *Functional interface.*
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/attic/generate_conrune_deltas.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_conrune()` - *Functional interface.*
**Exposes:** `ternary_to_decimal()` - *Functional interface.*
**Exposes:** `generate_deltas()` - *Functional interface.*


---

**File:** `scripts/attic/generate_constellation_report.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/attic/generate_cycle_nomenclature.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `datetime.timedelta`
* `math`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_charge()` - *Functional interface.*
**Exposes:** `get_sign_code()` - *Functional interface.*
**Exposes:** `generate_nomenclature()` - *Functional interface.*


---

**File:** `scripts/attic/generate_grimoire.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `csv`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_grimoire()` - *Functional interface.*


---

**File:** `scripts/attic/generate_kairos_names.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `datetime.timedelta`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `random`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_charge()` - *Functional interface.*
**Exposes:** `generate_name()` - *Generate a unique poetic name based on physics.*
**Exposes:** `generate_kairoi()` - *Functional interface.*


---

**File:** `scripts/attic/generate_organic_constellations.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `random`
* `sys`
* `time`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_organic_constellations()` - *Functional interface.*
**Exposes:** `print_grid()` - *Functional interface.*


---

**File:** `scripts/attic/generate_planetary_lattices.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `os`
* `random`
* `sys`
* `time`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_planetary_lattices()` - *Functional interface.*


---

**File:** `scripts/attic/generate_prompts.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_prompts()` - *Functional interface.*


---

**File:** `scripts/attic/generate_source_docs.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `ast`
* `logging`
* `os`
* `pathlib.Path`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `infer_architecture()` - *Functional interface.*
**Exposes:** `analyze_file()` - *Functional interface.*
**Exposes:** `generate_mermaid()` - *Functional interface.*
**Exposes:** `generate_markdown()` - *Functional interface.*
**Exposes:** `main()` - *Functional interface.*
**Exposes:** `visit_Import()` - *Functional interface.*
**Exposes:** `visit_ImportFrom()` - *Functional interface.*
**Exposes:** `visit_ClassDef()` - *Functional interface.*
**Exposes:** `visit_FunctionDef()` - *Functional interface.*
**Exposes:** `calculate_complexity()` - *Functional interface.*
**Exposes:** `visit_Call()` - *Functional interface.*
**Exposes:** `visit_Assign()` - *Functional interface.*


---

**File:** `scripts/attic/generate_spiral_deltas.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `datetime.timedelta`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_conrune()` - *Functional interface.*
**Exposes:** `ternary_to_decimal()` - *Functional interface.*
**Exposes:** `generate_spiral_deltas()` - *Functional interface.*


---

**File:** `scripts/attic/generate_spiral_matrix.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `datetime.timedelta`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_conrune_ternary()` - *Functional interface.*
**Exposes:** `ternary_to_decimal()` - *Functional interface.*
**Exposes:** `generate_spiral_matrix()` - *Functional interface.*


---

**File:** `scripts/attic/generate_star_catalog.py`

**Role:** `[Scout]`

**Purpose:** Generate The Catalog of Fixed Stars v2.0

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections.defaultdict`
* `csv`
* `json`
* `os`
* `random`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `load_lattices()` - *Functional interface.*
**Exposes:** `load_wall_values()` - *Load numeric values for each wall.*
**Exposes:** `calculate_seeds()` - *Calculate seed coordinates for a planetary lattice.*
**Exposes:** `find_void_and_seed_coords()` - *Find all void coords and all seed coords.*
**Exposes:** `distribute_voids_to_walls()` - *Distribute 41 void coords evenly across 7 walls (6,6,6,6,6,6,5).*
**Exposes:** `generate_name()` - *Generate a unique mythical name.*
**Exposes:** `get_etymology()` - *Get etymology for a name.*
**Exposes:** `generate_void_nature()` - *Generate nature description for a void star.*
**Exposes:** `generate_seed_nature()` - *Generate nature description for a seed star.*
**Exposes:** `generate_void_whisper()` - *Functional interface.*
**Exposes:** `generate_seed_whisper()` - *Functional interface.*
**Exposes:** `generate_catalog()` - *Generate the full star catalog.*
**Exposes:** `write_markdown()` - *Write the catalog as Markdown.*
**Exposes:** `write_json()` - *Write the catalog as JSON.*


---

**File:** `scripts/attic/reverse_engineer_seeds.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `reverse_engineer()` - *Functional interface.*


---

**File:** `scripts/attic/visualize_constellation_preview.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* None.

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `text_to_grid()` - *Functional interface.*
**Exposes:** `render_stars_only()` - *Functional interface.*
**Exposes:** `render_asterism_cardinal()` - *Functional interface.*


---

**File:** `scripts/attic/visualize_hexameric_split.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `visualize_hexameric_split()` - *Functional interface.*


---

**File:** `scripts/attic/visualize_seeds_atlas.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `json`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_vis()` - *Functional interface.*


---

**File:** `scripts/attic/visualize_structure_options.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `visualize_structure()` - *Functional interface.*


---

**File:** `scripts/attic/visualize_wall_octets.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections.defaultdict`
* `csv`
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `visualize_octets()` - *Functional interface.*


---

**File:** `scripts/audit_documentation_coverage.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pathlib.Path`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `audit_documentation()` - *Functional interface.*


---

**File:** `scripts/audit_mythos_length.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `audit_mythos_length()` - *Functional interface.*


---

**File:** `scripts/automate_inscription.py`

**Role:** `[Scout]`

**Purpose:** Automate Inscription Engine for IsopGem - The Ritual of Connection.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `ast`
* `collections.defaultdict`
* `datetime.datetime`
* `os`
* `pathlib.Path`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_file_metadata()` - *Parses a Python file to extract metadata for documentation.*
**Exposes:** `update_documentation()` - *Updates the REFERENCE.md file, replacing existing entries or appending new ones.*
**Exposes:** `inscribe_pillar()` - *Functional interface.*
**Exposes:** `inscribe_manifest()` - *Functional interface.*
**Exposes:** `main()` - *Functional interface.*
**Exposes:** `survey()` - *Perform a total scan of Python files.*
**Exposes:** `get_consumers()` - *Finds who imports the given file.*


---

**File:** `scripts/covenant_scripts/awaken.py`

**Role:** `[Scout]`

**Purpose:** The Rite of Awakening (awaken.py)

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pathlib.Path`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `read_scroll()` - *Functional interface.*
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/conversions.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `typing.Tuple`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `to_zodiacal_string()` - *Convert absolute degree (0-360) to zodiacal degree notation (Deg Sign Min).*


---

**File:** `scripts/deep_wall_analysis.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `os`
* `pillars.adyton.models.kamea_cell.KameaCell`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_deep()` - *Functional interface.*
**Exposes:** `get_wall()` - *Functional interface.*


---

**File:** `scripts/diagnose_window_flags.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* `name`
* `flags`
* `parent`

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `PyQt6.QtWidgets.QLabel`
* `PyQt6.QtWidgets.QMainWindow`
* `PyQt6.QtWidgets.QPushButton`
* `PyQt6.QtWidgets.QVBoxLayout`
* `PyQt6.QtWidgets.QWidget`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `setup_ui()` - *Functional interface.*


---

**File:** `scripts/download_de441.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `skyfield.api.load`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/explore_cluster_variants.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `explore_variants()` - *Functional interface.*
**Exposes:** `print_grid()` - *Functional interface.*


---

**File:** `scripts/export_amun_data.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `os`
* `pathlib.Path`
* `pillars.tq.models.amun_sound.AmunSoundCalculator`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `export_amun_data()` - *Functional interface.*


---

**File:** `scripts/find_cluster_split.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `find_cluster_split()` - *Functional interface.*


---

**File:** `scripts/find_faces.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `dist()` - *Functional interface.*


---

**File:** `scripts/get_constellation_metrics.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `csv`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_constellation_metrics()` - *Functional interface.*


---

**File:** `scripts/get_dead_zone_values.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `csv`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_dead_zone_values()` - *Functional interface.*


---

**File:** `scripts/get_growth_cycle.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `csv`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_growth_cycle()` - *Functional interface.*


---

**File:** `scripts/get_seed_values.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `csv`
* `json`
* `os`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_seed_values()` - *Functional interface.*


---

**File:** `scripts/horizon_seals/seal_jupiter.py`

**Role:** `[Scout]`

**Purpose:** The Seal of Jupiter: Performance & Expansion.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `datetime.timedelta`
* `logging`
* `pillars.astrology.models.chart_models.AstrologyEvent`
* `pillars.astrology.models.chart_models.ChartRequest`
* `pillars.astrology.models.chart_models.GeoLocation`
* `pillars.astrology.services.openastro_service.OpenAstroService`
* `statistics.mean`
* `statistics.stdev`
* `time`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_seal()` - *Functional interface.*


---

**File:** `scripts/horizon_seals/seal_mars.py`

**Role:** `[Scout]`

**Purpose:** The Seal of Mars: Resilience & Fuzzing.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `pillars.astrology.models.chart_models.AstrologyEvent`
* `pillars.astrology.models.chart_models.ChartRequest`
* `pillars.astrology.models.chart_models.GeoLocation`
* `pillars.astrology.services.openastro_service.ChartComputationError`
* `pillars.astrology.services.openastro_service.OpenAstroService`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_seal()` - *Functional interface.*


---

**File:** `scripts/horizon_seals/seal_sun.py`

**Role:** `[Scout]`

**Purpose:** The Seal of the Sun: Truth & Accuracy.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `datetime.timezone`
* `math`
* `pillars.astrology.models.chart_models.AstrologyEvent`
* `pillars.astrology.models.chart_models.ChartRequest`
* `pillars.astrology.models.chart_models.GeoLocation`
* `pillars.astrology.services.openastro_service.OpenAstroService`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_seal()` - *Functional interface.*


---

**File:** `scripts/init_mindscape_db.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.document_manager.models.MindEdge`
* `pillars.document_manager.models.MindNode`
* `shared.database.Base`
* `shared.database.engine`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `init_db()` - *Functional interface.*


---

**File:** `scripts/inspect_ribbon_api.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `inspect`
* `pyqtribbon.panel.RibbonPanel`
* `pyqtribbon`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/migrate_document_images.py`

**Role:** `[Scout]`

**Purpose:** Migration script to extract embedded images from existing documents.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `pathlib.Path`
* `pillars.document_manager.models.document.DocumentImage`
* `pillars.document_manager.models.document.Document`
* `pillars.document_manager.repositories.image_repository.ImageRepository`
* `pillars.document_manager.utils.image_utils.count_embedded_images`
* `pillars.document_manager.utils.image_utils.extract_images_from_html`
* `pillars.document_manager.utils.image_utils.has_embedded_images`
* `shared.database.Base`
* `shared.database.engine`
* `shared.database.get_db_session`


**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `create_tables()` - *Ensure the document_images table exists.*
**Exposes:** `migrate_document()` - *Migrate a single document's images.*
**Exposes:** `migrate_all()` - *Migrate all documents with embedded images.*
**Exposes:** `store_image()` - *Functional interface.*


---

**File:** `scripts/migrate_gematria_whoosh_to_sqlite.py`

**Role:** `[Scout]`

**Purpose:** Import existing Whoosh-stored gematria calculations into SQLite.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `__future__.annotations`
* `argparse`
* `datetime.datetime`
* `json`
* `pathlib.Path`
* `pillars.gematria.models.CalculationRecord`
* `pillars.gematria.repositories.sqlite_calculation_repository.SQLiteCalculationRepository`
* `shared.database.init_db`
* `typing.Iterable`
* `typing.List`
* `whoosh.index`
* `whoosh.query.Every`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `load_whoosh_records()` - *Yield every calculation stored inside the legacy Whoosh index.*
**Exposes:** `migrate()` - *Run the migration.*
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/octant_distribution_law.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `analyze_octant_law()` - *Functional interface.*
**Exposes:** `get_wall()` - *Functional interface.*


---

**File:** `scripts/parse_grimoire.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `os`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `parse_grimoire()` - *Functional interface.*


---

**File:** `scripts/populate_interpretations.py`

**Role:** `[Scout]`

**Purpose:** The Grand Scribe.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `pathlib.Path`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `generate_sign_content()` - *Functional interface.*
**Exposes:** `generate_house_content()` - *Functional interface.*
**Exposes:** `populate_signs()` - *Functional interface.*
**Exposes:** `populate_houses()` - *Functional interface.*


---

**File:** `scripts/regenerate_prompts.py`

**Role:** `[Scout]`

**Purpose:** Regenerate The Book of Prompts with improved formatting.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `re`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `normalize_planet()` - *Normalize planet name to uppercase key.*
**Exposes:** `create_improved_prompt()` - *Create an improved prompt with portrait orientation and enhanced structure.*
**Exposes:** `parse_and_regenerate()` - *Parse existing prompts and regenerate with improved format.*


---

**File:** `scripts/repair_quad_shape.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `pathlib.Path`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `repair()` - *Functional interface.*


---

**File:** `scripts/reset_database.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pathlib.Path`
* `shutil`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `reset_database()` - *Deletes the Whoosh index directory to reset the database.*


---

**File:** `scripts/rite_of_sovereignty.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `re`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `check_sovereignty()` - *Functional interface.*


---

**File:** `scripts/rite_of_zodiac.py`

**Role:** `[Scout]`

**Purpose:** THE RITE OF THE ZODIAC

**Input (Ingests):**
* `target_module_str`

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `coverage`
* `importlib`
* `inspect`
* `os`
* `random`
* `string`
* `sys`
* `threading`
* `time`
* `tracemalloc`
* `typing.Any`
* `typing.Callable`
* `typing.List`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `speak()` - *Functional interface.*
**Exposes:** `seal()` - *Functional interface.*
**Exposes:** `load_target()` - *Functional interface.*
**Exposes:** `run_aries()` - *ARIES (The Ram): Initialization Speed*
**Exposes:** `run_leo()` - *LEO (The Lion): CPU Performance*
**Exposes:** `run_sagittarius()` - *SAGITTARIUS (The Archer): Integration Flow*
**Exposes:** `run_taurus()` - *TAURUS (The Bull): Schema & Persistence*
**Exposes:** `run_virgo()` - *VIRGO (The Virgin): Purity & Static Analysis*
**Exposes:** `run_capricorn()` - *CAPRICORN (The Goat): Legacy & Regression*
**Exposes:** `run_gemini()` - *GEMINI (The Twins): API Contracts*
**Exposes:** `run_libra()` - *LIBRA (The Scales): Memory Balance*
**Exposes:** `run_aquarius()` - *AQUARIUS (The Water Bearer): Concurrency*
**Exposes:** `run_cancer()` - *CANCER (The Crab): Security & Isolation*
**Exposes:** `run_scorpio()` - *SCORPIO (The Scorpion): Chaos & Mutation*
**Exposes:** `run_pisces()` - *PISCES (The Fish): Completeness (Coverage)*
**Exposes:** `execute()` - *Functional interface.*
**Exposes:** `access_module()` - *Functional interface.*


---

**File:** `scripts/scaffold_interpretations.py`

**Role:** `[Scout]`

**Purpose:** The Scaffolding Scribe.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `json`
* `pathlib.Path`
* `typing.Any`
* `typing.Dict`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `load_or_create()` - *Functional interface.*
**Exposes:** `save_json()` - *Functional interface.*
**Exposes:** `scaffold_signs()` - *Functional interface.*
**Exposes:** `scaffold_houses()` - *Functional interface.*
**Exposes:** `scaffold_combinatorial()` - *Functional interface.*


---

**File:** `scripts/scribe_of_thoth.py`

**Role:** `[Scout]`

**Purpose:** The Scribe of Thoth - Automated Inscription Tool.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `ast`
* `pathlib.Path`
* `re`
* `sys`
* `typing.List`
* `typing.Optional`
* `typing.Tuple`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `camel_to_spaces()` - *Convert CamelCase to 'Camel Case'.*
**Exposes:** `snake_to_sentence()` - *Convert snake_case to 'Sentence case'.*
**Exposes:** `generate_docstring()` - *Generate a skeleton docstring for a node.*
**Exposes:** `process_file()` - *Inject or update docstrings into a file. Returns count of inscriptions.*
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/solve_interference.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`
* `numpy`
* `os`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `load_matrix()` - *Functional interface.*
**Exposes:** `solve()` - *Functional interface.*


---

**File:** `scripts/sync_wiki.py`

**Role:** `[Scout]`

**Purpose:** Wiki Synchronization Engine for IsopGem - Universal Manifestation Ritual.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `ast`
* `os`
* `pathlib.Path`
* `shutil`
* `subprocess`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_command()` - *Functional interface.*
**Exposes:** `get_docstring()` - *Extracts the first docstring from a Python file.*
**Exposes:** `transmute_name()` - *Converts a local path to a flat GitHub Wiki filename.*
**Exposes:** `generate_manifest()` - *Generates a manifest page for repository directories (scripts, tests).*
**Exposes:** `sync()` - *Functional interface.*


---

**File:** `scripts/test_audio_aplay.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.tq.services.amun_audio_service.AmunAudioService`
* `subprocess`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_playback()` - *Functional interface.*


---

**File:** `scripts/universal_pattern_search.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `collections`
* `os`
* `pillars.adyton.models.kamea_cell.KameaCell`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `pillars.tq.services.ternary_service.TernaryService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_wall_index()` - *Functional interface.*
**Exposes:** `analyze_conrune_invariance()` - *Functional interface.*


---

**File:** `scripts/update_db_schema.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `sqlite3`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `add_column()` - *Functional interface.*
**Exposes:** `update_schema()` - *Functional interface.*


---

**File:** `scripts/verify_batch_send.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.gematria.ui.batch_calculator_window.BatchCalculatorWindow`
* `sys`
* `unittest.mock.MagicMock`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_send_to_emerald()` - *Functional interface.*
**Exposes:** `open_window_side_effect()` - *Functional interface.*


---

**File:** `scripts/verify_boolean.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.correspondences.services.formula_engine.FormulaEngine`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_boolean()` - *Functional interface.*


---

**File:** `scripts/verify_columnar_symmetry.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_conrune()` - *Functional interface.*
**Exposes:** `verify_grid_symmetry()` - *Functional interface.*


---

**File:** `scripts/verify_conditional.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.correspondences.services.conditional_formatting.ConditionalManager`
* `pillars.correspondences.services.conditional_formatting.ConditionalRule`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_conditional()` - *Functional interface.*


---

**File:** `scripts/verify_context_menu.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QPoint`
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `PyQt6.QtWidgets.QMenu`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_context_menu()` - *Functional interface.*
**Exposes:** `mocked_exec()` - *Functional interface.*


---

**File:** `scripts/verify_copy_paste.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QItemSelectionModel`
* `PyQt6.QtCore.Qt`
* `PyQt6.QtGui.QClipboard`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_copy_paste()` - *Functional interface.*


---

**File:** `scripts/verify_copy_values.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QItemSelectionModel`
* `PyQt6.QtCore.Qt`
* `PyQt6.QtGui.QClipboard`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_copy_values()` - *Functional interface.*


---

**File:** `scripts/verify_csv_io.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `PyQt6.QtWidgets.QFileDialog`
* `PyQt6.QtWidgets.QMessageBox`
* `csv`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`
* `unittest.mock.patch`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_csv_io()` - *Functional interface.*


---

**File:** `scripts/verify_db_manager.py`

**Role:** `[Scout]`

**Purpose:** Verification script for Document Database Manager logic.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `logging`
* `os`
* `src.pillars.document_manager.models.document.DocumentImage`
* `src.pillars.document_manager.models.document.Document`
* `src.pillars.document_manager.services.document_service.DocumentService`
* `src.shared.database.get_db_session`
* `sys`
* `traceback`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_db_manager()` - *Functional interface.*


---

**File:** `scripts/verify_delete_key.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QItemSelectionModel`
* `PyQt6.QtCore.Qt`
* `PyQt6.QtGui.QKeyEvent`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_delete_key()` - *Functional interface.*


---

**File:** `scripts/verify_etymology.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `src.pillars.document_manager.services.etymology_service.get_etymology_service`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_etymology()` - *Functional interface.*


---

**File:** `scripts/verify_filter_fix.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* `id`
* `title`
* `collection`

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `sys`
* `unittest.mock.MagicMock`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_filter_fix()` - *Functional interface.*


---

**File:** `scripts/verify_fixed_stars_panel.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `datetime.datetime`
* `os`
* `pillars.astrology.models.chart_models.ChartResult`
* `pillars.astrology.models.chart_models.PlanetPosition`
* `pillars.astrology.services.fixed_stars_service.FixedStarsService`
* `pillars.astrology.ui.advanced_analysis_panel.AdvancedAnalysisPanel`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_logic()` - *Functional interface.*


---

**File:** `scripts/verify_formula_range.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.correspondences.services.formula_engine.FormulaEngine`
* `sys`
* `typing.Any`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_range_formula()` - *Functional interface.*
**Exposes:** `get_cell_value()` - *Functional interface.*


---

**File:** `scripts/verify_gematria_send.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.gematria.services.base_calculator.GematriaCalculator`
* `pillars.gematria.ui.gematria_calculator_window.GematriaCalculatorWindow`
* `sys`
* `unittest.mock.MagicMock`
* `unittest.mock.patch`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_send_to_emerald()` - *Functional interface.*
**Exposes:** `name()` - *Functional interface.*
**Exposes:** `calculate()` - *Functional interface.*
**Exposes:** `get_breakdown()` - *Functional interface.*
**Exposes:** `normalize_text()` - *Functional interface.*
**Exposes:** `open_window_side_effect()` - *Functional interface.*


---

**File:** `scripts/verify_ghosts.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `sqlite3`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `check_ghosts()` - *Functional interface.*


---

**File:** `scripts/verify_help.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `logging`
* `os`
* `pathlib.Path`
* `shared.services.help_service.HelpService`
* `shared.ui.help_window.HelpWindow`
* `sys`
* `traceback`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_help_system()` - *Functional interface.*


---

**File:** `scripts/verify_hestia.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.geometry.services.VaultOfHestiaShape`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/verify_hestia_bidirectional.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.geometry.services.vault_of_hestia_shape.VaultOfHestiaShape`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/verify_hestia_expanded.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.geometry.services.vault_of_hestia_shape.VaultOfHestiaShape`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/verify_horizon_phase1.py`

**Role:** `[Scout]`

**Purpose:** Verification Script for Horizon Phase 1 (Astrometric Integrity).

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `datetime.timezone`
* `os`
* `pillars.astrology.models.chart_models.GeoLocation`
* `pillars.astrology.repositories.ephemeris_provider.EphemerisProvider`
* `sys`
* `time`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_verification()` - *Functional interface.*


---

**File:** `scripts/verify_horizon_phase2.py`

**Role:** `[Scout]`

**Purpose:** Verification Script for Horizon Phase 2 (Chart Breadth).

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.datetime`
* `datetime.timezone`
* `os`
* `pillars.astrology.models.chart_models.AstrologyEvent`
* `pillars.astrology.models.chart_models.ChartRequest`
* `pillars.astrology.models.chart_models.ChartResult`
* `pillars.astrology.models.chart_models.GeoLocation`
* `pillars.astrology.models.chart_models.PlanetPosition`
* `pillars.astrology.services.aspects_service.AspectsService`
* `pillars.astrology.services.openastro_service.OpenAstroService`
* `pillars.astrology.services.progressions_s

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_verification()` - *Functional interface.*
**Exposes:** `generate_chart()` - *Functional interface.*


---

**File:** `scripts/verify_horizon_phase3.py`

**Role:** `[Scout]`

**Purpose:** Verification Script for Horizon Phase 3 (Interpretation & Reporting).

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pathlib.Path`
* `pillars.astrology.models.interpretation_models.InterpretationReport`
* `pillars.astrology.models.interpretation_models.RichInterpretationContent`
* `pillars.astrology.repositories.interpretation_repository.InterpretationRepository`
* `pillars.astrology.services.chart_storage_service.ChartStorageService`
* `pillars.astrology.services.report_service.ReportService`
* `sys`
* `tempfile`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_verification()` - *Functional interface.*


---

**File:** `scripts/verify_horizon_phase4.py`

**Role:** `[Scout]`

**Purpose:** Verification Script for Horizon Phase 4 (UX & Performance).

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QCoreApplication`
* `PyQt6.QtCore.QThreadPool`
* `logging`
* `os`
* `pillars.astrology.models.chart_models.AstrologyEvent`
* `pillars.astrology.models.chart_models.ChartRequest`
* `pillars.astrology.models.chart_models.GeoLocation`
* `pillars.astrology.services.openastro_service.OpenAstroService`
* `shared.logging_config.configure_logging`
* `shared.ui.worker.BackgroundWorker`
* `sys`
* `time`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_worker_function()` - *Functional interface.*
**Exposes:** `run_verification()` - *Functional interface.*
**Exposes:** `on_result()` - *Functional interface.*
**Exposes:** `on_finished()` - *Functional interface.*
**Exposes:** `generate_chart_internal()` - *Functional interface.*


---

**File:** `scripts/verify_horizon_phase5.py`

**Role:** `[Scout]`

**Purpose:** The Final Rite: Verifying the Horizon.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `horizon_seals.seal_jupiter`
* `horizon_seals.seal_mars`
* `horizon_seals.seal_sun`
* `os`
* `shutil`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/verify_keyboard_nav.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QEvent`
* `PyQt6.QtCore.QPoint`
* `PyQt6.QtCore.Qt`
* `PyQt6.QtGui.QKeyEvent`
* `PyQt6.QtGui.QMouseEvent`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_keyboard_nav()` - *Functional interface.*


---

**File:** `scripts/verify_kinetic_enforcer.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QEvent`
* `PyQt6.QtCore.QObject`
* `PyQt6.QtWidgets.QApplication`
* `PyQt6.QtWidgets.QPushButton`
* `shared.ui.kinetic_enforcer.KineticEnforcer`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/verify_library_ui.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.document_manager.ui.document_library.DocumentLibrary`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `main()` - *Functional interface.*


---

**File:** `scripts/verify_manifest.py`

**Role:** `[Scout]`

**Purpose:** The Automated Sentinel - Guardian of the Akaschic Record.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pathlib.Path`
* `re`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `log_error()` - *Functional interface.*
**Exposes:** `log_info()` - *Functional interface.*
**Exposes:** `verify_pillar()` - *Functional interface.*
**Exposes:** `verify_file_integrity()` - *Functional interface.*
**Exposes:** `run()` - *Functional interface.*
**Exposes:** `summary()` - *Functional interface.*


---

**File:** `scripts/verify_orbital_physics.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `PyQt6.QtWidgets.QMainWindow`
* `PyQt6.QtWidgets.QWidget`
* `shared.ui.window_manager.WindowManager`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/verify_parser.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `pillars.correspondences.services.formula_engine.FormulaEngine`
* `pillars.correspondences.services.formula_engine.Parser`
* `pillars.correspondences.services.formula_engine.Tokenizer`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `check()` - *Functional interface.*
**Exposes:** `get_cell_value()` - *Functional interface.*


---

**File:** `scripts/verify_planetary_send.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `datetime.datetime`
* `json`
* `os`
* `pillars.astrology.ui.planetary_positions_window.EphemerisRow`
* `pillars.astrology.ui.planetary_positions_window.PlanetaryPositionsWindow`
* `pillars.correspondences.ui.spreadsheet_view.SpreadsheetModel`
* `pillars.gematria.ui.batch_calculator_window.BatchCalculatorWindow`
* `sys`
* `unittest.mock.MagicMock`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_send_to_emerald()` - *Functional interface.*
**Exposes:** `test_batch_send()` - *Functional interface.*
**Exposes:** `open_window_side_effect()` - *Functional interface.*
**Exposes:** `open_window_side_effect()` - *Functional interface.*


---

**File:** `scripts/verify_polyline.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.geometry.ui.primitives.BrushStyle`
* `pillars.geometry.ui.primitives.PolygonPrimitive`
* `pillars.geometry.ui.scene_adapter.build_scene_payload`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
* Internal logic only.


---

**File:** `scripts/verify_seed_of_life.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.geometry.services.seed_of_life_shape.SeedOfLifeShape`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_tests()` - *Functional interface.*


---

**File:** `scripts/verify_send_to.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.repos.table_repository.TableRepository`
* `pillars.correspondences.ui.correspondence_hub.CorrespondenceHub`
* `shared.database.get_db_session`
* `shared.ui.window_manager.WindowManager`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_send_to()` - *Functional interface.*


---

**File:** `scripts/verify_sorting.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.Qt`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_sorting()` - *Functional interface.*


---

**File:** `scripts/verify_string_functions.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.correspondences.services.formula_engine.FormulaEngine`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_string_functions()` - *Functional interface.*
**Exposes:** `chk()` - *Functional interface.*


---

**File:** `scripts/verify_text_analysis_send.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.gematria.services.base_calculator.GematriaCalculator`
* `pillars.gematria.ui.text_analysis.main_window.TextAnalysisWindow`
* `sys`
* `unittest.mock.MagicMock`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `test_send_search_to_emerald()` - *Functional interface.*
**Exposes:** `open_window_side_effect()` - *Functional interface.*


---

**File:** `scripts/verify_textjoin_none.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.correspondences.services.formula_engine.FormulaEngine`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_textjoin_none()` - *Functional interface.*


---

**File:** `scripts/verify_three_laws.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `datetime.timedelta`
* `numpy`
* `os`
* `pillars.time_mechanics.services.tzolkin_service.TzolkinService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `get_conrune()` - *Functional interface.*
**Exposes:** `swap_trigrams()` - *Functional interface.*
**Exposes:** `verify_laws()` - *Functional interface.*


---

**File:** `scripts/verify_torus.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.geometry.services.torus_solid.TorusMeshConfig`
* `pillars.geometry.services.torus_solid.TorusSolidCalculator`
* `pillars.geometry.services.torus_solid.TorusSolidService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_tests()` - *Functional interface.*


---

**File:** `scripts/verify_torus_knot.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `math`
* `os`
* `pillars.geometry.services.torus_knot_solid.TorusKnotMeshConfig`
* `pillars.geometry.services.torus_knot_solid.TorusKnotSolidCalculator`
* `pillars.geometry.services.torus_knot_solid.TorusKnotSolidService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `run_tests()` - *Functional interface.*


---

**File:** `scripts/verify_tzolkin.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `csv`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_tzolkin_sum()` - *Functional interface.*


---

**File:** `scripts/verify_ui_startup.py`

**Role:** `[Scout]`

**Purpose:** UI Startup Verification Script

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtWidgets.QApplication`
* `PyQt6.QtWidgets.QWidget`
* `importlib`
* `logging`
* `os`
* `shared.services.gematria.greek_calculator.GreekGematriaCalculator`
* `shared.services.gematria.hebrew_calculator.HebrewGematriaCalculator`
* `shared.services.gematria.tq_calculator.TQGematriaCalculator`
* `shared.signals.navigation_bus.WINDOW_REGISTRY`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_windows()` - *Functional interface.*


---

**File:** `scripts/verify_unified_edit.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `PyQt6.QtCore.QEvent`
* `PyQt6.QtCore.QPoint`
* `PyQt6.QtCore.Qt`
* `PyQt6.QtGui.QMouseEvent`
* `PyQt6.QtWidgets.QApplication`
* `os`
* `pillars.correspondences.ui.spreadsheet_window.SpreadsheetWindow`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_unified_edit()` - *Functional interface.*


---

**File:** `scripts/verify_wall_map_loading.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `os`
* `pillars.adyton.services.kamea_loader_service.KameaLoaderService`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `verify_map()` - *Functional interface.*


---

**File:** `scripts/wipe_mindscape.py`

**Role:** `[Scout]`

**Purpose:** Soul not yet specified.

**Input (Ingests):**
* Pure data structure or utility module.

**Output (Emits):**
* Data primitives or DTOs.

**Dependencies (It Needs):**
* `argparse`
* `os`
* `pillars.document_manager.models.mindscape.MindEdge`
* `pillars.document_manager.models.mindscape.MindNode`
* `shared.database.get_db_session`
* `sys`

**Consumers (Who Needs It):**
* None detected.

**Key Interactions:**
**Exposes:** `wipe()` - *Functional interface.*
